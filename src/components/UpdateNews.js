import { getFirestore, doc, updateDoc } from 'firebase/firestore';
import { db } from '../firebase';
// Import db from firebase.js

const UpdateNews = ({ id, currentTitle, currentContent, onUpdate }) => {
  // Your existing code
};

export default UpdateNews;
